public class CollectionPanel : MainMenuPanel
{
    public override void Initialize(ScreenUI screenUI)
    {
        base.Initialize(screenUI);
        InitUI();
    }

    private void InitUI()
    {
    }
}